import {useRef,useEffect,useLayoutEffect} from 'react'
const useBrowserLayoutEffect=typeof window==='undefined'?useEffect:useLayoutEffect
import type {BeatNex} from '../../features/useBeatNex'
import {DRUM_IDS} from '../../core/pattern/types'
import {meterForPattern} from '../../core/pattern/editor'
import {PHASE_GUIDES,phaseIds} from '../../storage/practice'
import {TempoWheel} from '../components/TempoWheel'
import {DrumIcon,Icon,drumNames,labelDrums} from '../components/Icons'
import {PhaseChipEditor} from '../PhaseChipEditor'
import {PracticeProgress} from '../components/PracticeProgress'
export function PracticePage({app}:{app:BeatNex}){
 const {settings,pattern,snapshot,playing,loading,phases,activeIndex,cycle,totalBars}=app
 const beats=useRef<HTMLDivElement>(null),marker=useRef<HTMLSpanElement>(null)
 const lastTap=useRef({key:'',at:0})
 const freeTime=Math.floor(snapshot.elapsed/60).toString().padStart(2,'0')+':'+Math.floor(snapshot.elapsed%60).toString().padStart(2,'0')
 const pulse=Math.floor(snapshot.progress*pattern.beatsPerBar*2),guide=PHASE_GUIDES[phaseIds.indexOf(phases[activeIndex].id as never)].replace('{drum}',labelDrums(settings.targets))
 useBrowserLayoutEffect(()=>{const root=beats.current,node=marker.current;if(!root||!node)return;const place=()=>{const cell=root.querySelectorAll<HTMLElement>('.bn-count')[pulse];if(!cell)return;const a=root.getBoundingClientRect(),b=cell.getBoundingClientRect();Object.assign(node.style,{width:b.width+'px',height:b.height+'px',transform:`translate(${b.left-a.left}px,${b.top-a.top}px)`,background:pulse%2?'var(--lilac)':'var(--acid)'});};place();const observer=new ResizeObserver(place);observer.observe(root);return()=>observer.disconnect()},[pulse,pattern.beatsPerBar,app.page])
 return <section className="bn-page" data-view="practice" hidden={app.page!=='practice'}><PracticeProgress total={totalBars} cycle={cycle} snapshot={snapshot} complete={app.completed} round={app.round} infinite={settings.repeat==='infinite'} enabled={settings.routeEnabled} guide={settings.targets.length?guide:'点选需要的鼓件；双击可取消选择'}/><div className="bn-layout"><article className="bn-card bn-session"><div><div className="bn-row"><span className="bn-tag">{app.activeId?'我的组合':'BOOM BAP / HIP-HOP'}</span><span className="bn-live"><i className="bn-dot"/><span id="bn-status">{loading?'正在加载鼓组':playing?'正在练习':snapshot.status==='paused'?'已暂停':'准备就绪'}</span></span></div><h2 id="bn-pattern-title">{pattern.name}</h2><div className="bn-sub"><span id="bn-current-drum">{labelDrums(settings.targets)}</span><span id="bn-practice-meter">{meterForPattern(pattern)}</span><span className="bn-duration-slot"><span id="bn-duration">约 {Math.round(totalBars*60*pattern.beatsPerBar*4/(pattern.beatUnit??4)/settings.bpm)} 秒</span><span id="bn-free-session" hidden={settings.routeEnabled}>已练习 <b id="bn-free-time">{freeTime}</b></span></span></div></div><div><div ref={beats} className="bn-beats" aria-label={meterForPattern(pattern)+' 节拍提示'} style={{gridTemplateColumns:`repeat(${Math.min(6,pattern.beatsPerBar)},minmax(0,1fr))`}}><span ref={marker} className="bn-count-marker" aria-hidden="true"/>{Array.from({length:pattern.beatsPerBar},(_,i)=><div className={'bn-beat '+(playing&&Math.floor(pulse/2)===i?'on':'')} key={i}><b className={'bn-count '+(playing&&pulse===i*2?'is-current':'')}>{i+1}</b><span className={'bn-count bn-and '+(playing&&pulse===i*2+1?'is-current':'')}>&amp;</span></div>)}</div><div className="bn-row"><button className="bn-primary" id="bn-play" onClick={app.togglePlayback} disabled={loading}><Icon name={playing?'pause':'play'}/><span>{loading?'加载中…':playing?'暂停练习':snapshot.status==='paused'?'继续练习':'开始练习'}</span></button><button id="bn-free-end" aria-label="结束自由练习" onClick={app.endFree}>结束</button><button className="bn-smallbtn" id="bn-reset" aria-label="重新开始练习" onClick={()=>{app.reset();app.notice('已回到练习起点')}}><Icon name="reset"/></button></div></div><div className="bn-in-player"><article className="bn-card bn-tempo"><TempoWheel value={settings.bpm} min={60} max={140} onChange={app.changeBpm} label="练习速度"/><div className="bn-row bn-muted"><span>60 · 慢慢来</span><span>140 · 加点速</span></div></article><article className="bn-card bn-instruments"><div className="bn-drums" id="bn-drums" aria-describedby="bn-drum-help">{DRUM_IDS.map((drum,i)=><button className="bn-drum" key={drum} data-drum={i} aria-pressed={settings.targets.includes(drum)} onClick={e=>{const now=performance.now(),double=e.detail>=2||(e.detail!==0&&lastTap.current.key===drum&&now-lastTap.current.at<340);lastTap.current={key:double?'':drum,at:now};app.selectDrum(drum,double)}} onKeyDown={e=>{if(e.key==='Delete'||e.key==='Backspace'){e.preventDefault();app.selectDrum(drum,true)}}}><DrumIcon index={i}/><span>{drumNames[i]}</span></button>)}</div><div id="bn-drum-help">单击选择 · 双击取消</div></article></div></article>
 <article className="bn-card bn-course" aria-label="练习路线编排"><div className="bn-row"><div><h3>练习路线</h3><span className="bn-muted" id="bn-route-hint">长按拖动 · 拖入垃圾桶删除</span></div><div className="bn-repeat-control" role="group" aria-label="路线循环模式" hidden={!settings.routeEnabled}><i aria-hidden="true" style={{transform:`translateX(${settings.repeat==='infinite'?100:0}%)`}}/>{(['once','infinite'] as const).map(mode=><button key={mode} type="button" aria-label={mode==='once'?'循环一次':'无限循环'} title={mode==='once'?'循环一次':'无限循环'} aria-pressed={settings.repeat===mode} disabled={playing||loading} onClick={()=>app.changeRepeat(mode)}>{mode==='once'?'1×':'∞'}</button>)}</div><span hidden={settings.routeEnabled} className="bn-route-status" aria-hidden="true">{settings.routeEnabled?'已开启':'已关闭'}</span><button id="bn-route-toggle" role="switch" aria-checked={settings.routeEnabled} aria-label="启用练习路线" onClick={app.toggleRoute}><i/></button></div><p id="bn-free-hint" hidden={settings.routeEnabled}>练习路线已关闭</p><PhaseChipEditor phases={phases} locked={playing||loading||!settings.routeEnabled||app.page!=='practice'} activeIndex={activeIndex} onBarsChange={app.changeBars} onReorder={app.reorder} onDelete={app.removePhase} onAdd={app.addPhase} notice={app.notice}/></article></div></section>
}
